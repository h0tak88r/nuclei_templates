id: fuzzing-with-h0tak88r
info:
  name: Fuzzing Template
  author: h0tak88r
  severity: info

http:
  - method: GET
    path:
      - "{{BaseURL}}/{{fuzz}}"
    payloads:
      fuzz: /root/Recon88r/Wordlists/h0tak88r_fuzz.txt
    matchers:
      - type: status
        status:
          - 200
